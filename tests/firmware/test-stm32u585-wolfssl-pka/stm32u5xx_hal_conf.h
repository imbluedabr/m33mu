/* Minimal stm32u5xx_hal_conf.h for m33mu PKA firmware test.
 * All HAL types are provided by user_settings.h which is already
 * included by wolfSSL settings machinery before this header is needed. */
#ifndef STM32U5XX_HAL_CONF_H
#define STM32U5XX_HAL_CONF_H
/* intentionally empty — all needed definitions come from user_settings.h */
#endif /* STM32U5XX_HAL_CONF_H */
